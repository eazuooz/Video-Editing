import {makeProject} from '@motion-canvas/core';
import scene from './scene?scene';
import audio from './assets/sample-mix.wav';
// Isolated approval sample. Do not include in the full production until accepted.
export default makeProject({name:'let-them-play — Editorial 2.5D design sample',scenes:[scene],audio});
