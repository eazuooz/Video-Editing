import {makeProject} from '@motion-canvas/core';
import comparison from './switch-comparison?scene';
// Separate visual study; never included in the 1:1:1 main timeline automatically.
export default makeProject({name:'let-them-play — 2D vs 2.5D (silent sample)',scenes:[comparison]});
